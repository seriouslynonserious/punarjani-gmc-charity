import {Component,inject,AfterViewInit,NgZone,OnDestroy} from '@angular/core';
import {CommonModule} from '@angular/common';
import {FormGroup,FormControl,ReactiveFormsModule,Validators} from '@angular/forms';
import {ActivatedRoute,RouterLink} from '@angular/router';
import {Api,loadScript} from './api';
import {SCHEMAS,Schema,Field} from './schemas';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule,RouterLink],template:`
<section class="wrap page-hero"><a routerLink="/" class="breadcrumb">Home / Get involved</a><div class="eyebrow">ONE SMALL ACT STARTS HERE</div><h1>{{schema?.title || 'Form not found'}}</h1><p>{{schema?.description}}</p></section>
@if(schema){<div class="wrap content-grid"><section class="panel">@if(success){<div class="success" role="status"><div class="success-icon">✓</div><h2>Thank You ❤️</h2><p>Your information has been successfully submitted. Our student volunteer team will contact you if required.</p><p class="hint">Reference: {{reference}}</p><a routerLink="/" class="button">Back to home ↗</a></div>}@else{<form [formGroup]="form" (ngSubmit)="submit()" novalidate><p class="hint">Fields marked * are required.</p>@if(!available){<div class="note">Registrations open soon. You can explore this form, but submissions are not being collected yet.</div>}<div class="form-grid">@for(field of schema.fields;track field.key){<div class="field" [class.wide]="field.type==='textarea'||field.type==='checkbox'"><label [for]="field.key">{{field.label}}{{field.required?' *':''}}</label>@if(field.type==='select'){<select [id]="field.key" [formControlName]="field.key" [attr.aria-describedby]="field.key+'-error'" [attr.aria-invalid]="invalid(field)"><option value="">Select an option</option>@for(option of field.options;track option){<option [value]="option">{{option}}</option>}</select>}@else if(field.type==='textarea'){<textarea [id]="field.key" [formControlName]="field.key" maxlength="3000" [attr.aria-describedby]="field.key+'-error'" [attr.aria-invalid]="invalid(field)"></textarea>}@else{<input [id]="field.key" [type]="field.type" [formControlName]="field.key" [attr.min]="field.type==='number'?0:null" [attr.maxlength]="field.type==='text'?300:null" [attr.aria-describedby]="field.key+'-error'" [attr.aria-invalid]="invalid(field)">}@if(invalid(field)){<span class="field-error" [id]="field.key+'-error'">{{errorFor(field)}}</span>}</div>}</div><div class="honeypot" aria-hidden="true"><label for="website">Website</label><input id="website" formControlName="website" tabindex="-1" autocomplete="off"></div><label class="consent"><input type="checkbox" formControlName="Consent"><span>I agree to the <a routerLink="/privacy" class="text-link">privacy policy</a> and allow the charity team to contact me about this submission.@if(schema.key==='blood-donor'){ My first name, blood group, city and availability may appear in the donor directory.}</span></label>@if(form.controls['Consent'].touched&&form.controls['Consent'].invalid){<p class="field-error">Please give your consent before submitting.</p>}<div id="form-captcha"></div>@if(error){<p class="error" role="alert">{{error}}</p>}<button class="button" type="submit" [disabled]="busy||!available">{{busy?'Submitting…':'Submit '+(schema.key.includes('request')?'request':'registration')}} <span>↗</span></button><p class="hint">Your details are shared only with authorized charity volunteers, except the safe donor directory information described above.</p></form>}</section><aside><div class="panel"><div class="eyebrow">WHAT HAPPENS NEXT</div><h3>We take it from here.</h3><p>Our student volunteers review each submission and contact you if more information is needed.</p><hr><p class="hint">Support depends on availability. A submission does not guarantee assistance.</p>@if(schema.key==='blood-request'){<div class="note">For an immediate emergency, contact your hospital’s blood bank directly. This form is not monitored continuously.</div>}<a routerLink="/contact" class="text-link">Need to speak with us? ↗</a></div></aside></div>}
`})export class Registration implements AfterViewInit,OnDestroy{
 api=inject(Api);route=inject(ActivatedRoute);zone=inject(NgZone);schema:Schema|undefined;form=new FormGroup<Record<string,FormControl>>({});busy=false;success=false;error='';reference='';available=false;captcha='';proofMode=false;widget:any;id=crypto.randomUUID();started=Date.now();destroyed=false;
 constructor(){this.schema=SCHEMAS[this.route.snapshot.paramMap.get('kind')||''];if(this.schema){for(const f of this.schema.fields){const validators=[];if(f.required)validators.push(Validators.required);if(f.type==='email')validators.push(Validators.email);if(f.type==='tel')validators.push(Validators.pattern(/^\+?[0-9 ()-]{7,20}$/));if(f.type==='number'){validators.push(Validators.min(f.key==='Units'?1:0));if(f.key==='Age')validators.push(Validators.max(120));}validators.push(Validators.maxLength(f.type==='textarea'?3000:300));this.form.addControl(f.key,new FormControl(f.type==='checkbox'?false:'',validators))}this.form.addControl('Consent',new FormControl(false,Validators.requiredTrue));this.form.addControl('website',new FormControl(''));}}
 async ngAfterViewInit(){try{const c=await this.api.settings();this.proofMode=c.spamMode==='proof-of-work';this.available=!!(c.apiUrl&&(this.proofMode||c.turnstileSiteKey));if(this.available&&!this.proofMode){await loadScript('https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit');if(!this.destroyed)this.widget=(window as any).turnstile.render('#form-captcha',{sitekey:c.turnstileSiteKey,action:'submit',callback:(v:string)=>this.zone.run(()=>this.captcha=v),'expired-callback':()=>this.zone.run(()=>this.captcha='')});}}catch(e){this.error=(e as Error).message}}
 ngOnDestroy(){this.destroyed=true;if(this.widget!==undefined)(window as any).turnstile?.remove(this.widget)}
 invalid(f:Field){return this.form.controls[f.key].touched&&this.form.controls[f.key].invalid}
 errorFor(f:Field){return f.type==='email'?'Enter a valid email address.':f.type==='tel'?'Enter a valid phone number, including country code if needed.':f.type==='number'?'Enter a valid number within the allowed range.':'Please complete this field.'}
 async solveProof(){const challenge=await this.api.call('challenge',{kind:this.schema!.key,requestId:this.id});const encoder=new TextEncoder();for(let nonce=0;nonce<=4000000;nonce++){if(this.destroyed)throw Error('Submission cancelled.');const hash=new Uint8Array(await crypto.subtle.digest('SHA-256',encoder.encode(challenge.payload+':'+nonce)));if(hash[0]===0&&hash[1]===0)return {payload:challenge.payload,signature:challenge.signature,nonce};if(nonce%1000===0)await new Promise(r=>setTimeout(r,0));}throw Error('Security check could not finish. Please retry.');}
 async submit(){if(this.busy)return;this.form.markAllAsTouched();if(this.form.invalid){document.querySelector<HTMLElement>('[aria-invalid="true"]')?.focus();return}if(!this.proofMode&&!this.captcha){this.error='Please complete the security check.';return}this.busy=true;this.error='';try{const proof=this.proofMode?await this.solveProof():undefined;const r=await this.api.call('submit',{kind:this.schema!.key,values:this.form.value,requestId:this.id,startedAt:this.started,captcha:this.captcha,proof});this.reference=r.id;this.success=true;this.form.reset();}catch(e){this.error=(e as Error).message;(window as any).turnstile?.reset(this.widget);this.captcha=''}finally{this.busy=false}}
}
